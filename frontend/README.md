# Brain Tumor Chatbot Frontend

Tech stack: React + Vite, Three.js (React Three Fiber), Tailwind CSS, Framer Motion, Axios

Run:

```bash
cd frontend
npm install
npm run dev
```

It proxies `/api` to `http://localhost:8000` so ensure the backend is running.
